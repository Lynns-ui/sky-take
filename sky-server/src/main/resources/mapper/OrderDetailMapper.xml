<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insert">
        insert into order_detail (name,image,order_id,dish_id,setmeal_id,dish_flavor,number,amount)
        values
            <foreach collection="orderDetails" item="orderDetail" separator=",">
                (#{orderDetail.name},#{orderDetail.image},#{orderDetail.orderId},
                 #{orderDetail.dishId},#{orderDetail.setmealId},#{orderDetail.dishFlavor},#{orderDetail.number},#{orderDetail.amount})
            </foreach>
    </insert>

    <select id="getTop10" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name as name,count(*) number
        from order_detail od left join (select id,order_time from orders where status=5) o
        on od.order_id=o.id
        where o.order_time between #{beginTime} and #{endTime}
        group by od.name
        order by number DESC
        limit 0, 10
    </select>
</mapper>
